{
  "name": "naeessecurity",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "new-post": "node --import tsx scripts/new-post.ts",
    "optimize-images": "node --import tsx scripts/optimize-images.ts",
    "validate": "npm run optimize-images && npm run build"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "tsx": "^4.23.1"
  }
}
